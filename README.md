# Standoff 3 — PC Edition

Шутер от первого лица в духе Standoff 2 / Counter-Strike с ботами. Работает в браузере Chrome/Edge, ничего устанавливать не нужно.

## Десктоп-версия (exe / Mac) — рекомендуется
Скачайте архив из [Releases](https://github.com/Aldar67/standoff3/releases):
- **Windows** — `Standoff3-X.Y.Z-win64.zip`: распакуйте, запустите `Standoff3.exe`. Ничего устанавливать не нужно.
- **macOS** — `Standoff3-X.Y.Z-mac.zip`: распакуйте, двойной клик по `Standoff3-Mac.command` (первый раз: ПКМ → «Открыть»).
  Загрузчик сам скачает Electron под ваш процессор (Intel / Apple Silicon), соберёт `Standoff 3.app` рядом и запустит.

**Игра через интернет (VPS):** в файле `config.json` рядом с exe / .command укажите адрес своего сервера:
`{ "defaultServer": "ws://IP_VPS:8766" }` — он будет подставлен во вкладке «Игра по сети» и у хоста, и у остальных.
Для браузерной версии (запуск `index.html`) адрес лежит в `server-config.js` рядом с `index.html`.
Установка сервера на VPS (Ubuntu/Debian, от root): `scp server.py vps/install.sh root@IP_VPS:/root/ && ssh root@IP_VPS bash /root/install.sh`
— создаётся служба systemd `standoff3` и открывается порт 8766. Сервер поддерживает **комнаты**: хост задаёт название комнаты,
остальные выбирают её из списка «Открытые комнаты» — несколько компаний могут играть на одном VPS одновременно.

**Сборка десктоп-версии самому** (без Node.js): скачайте `electron-v44.4.3-win32-x64.zip` с github.com/electron/electron/releases и выполните
`python build_desktop.py путь/к/electron-v44.4.3-win32-x64.zip` — архивы появятся в `dist/`.

## Запуск в браузере
1. Дважды кликните **Standoff3.bat** — игра откроется в отдельном окне Chrome (или Edge) на весь экран.
2. Либо просто откройте `index.html` в Chrome/Edge.
3. В меню: «Играть» → выберите режим, карту, команду, число и сложность ботов → «Начать матч».
4. Кликните по экрану, чтобы захватить мышь. Esc — пауза, F11 — полный экран.

## Режимы
- **Разминирование** — классика: T ставят бомбу на A/B, CT защищают. Раунды, экономика ($), покупка оружия (B), смена сторон.
- **Командный бой** — команда на команду с возрождением до лимита убийств.
- **Каждый за себя** — все против всех.
- **Гонка вооружений** — каждое убийство даёт новое оружие, последний уровень — нож.

## Карты
Sandstone (пустыня, лонг/мид/тоннели), Rust (промзона, контейнеры), Province (старый город), Arena (для режимов с возрождением).

## Управление
WASD — движение, мышь — обзор, ЛКМ — огонь, ПКМ — прицел/удар ножом, Space — прыжок, Ctrl — присед, Shift — тихий шаг,
R — перезарядка, 1–5 — оружие, Q — предыдущее, E — обезвредить/подобрать, B — магазин, G — выбросить, F — осмотр, Tab — таблица, Z — радио.

## Игра по сети (LAN)
Один из игроков — хост, остальные подключаются к нему по локальной сети. Нужен Python (уже есть на компьютере хоста).

**Хост:**
1. Дважды кликните **Standoff3-Host.bat** — откроются два окна-сервера и покажут ваш IP-адрес в сети. Не закрывайте эти окна.
2. В игре откроется меню — нажмите «🌐 Игра по сети» → «Создать игру (хост)» → «Подключиться как хост».
3. Выберите режим, карту, ботов, свою команду, дождитесь в списке остальных игроков и нажмите «НАЧАТЬ ИГРУ ДЛЯ ВСЕХ».

**Остальные игроки:**
1. Откройте игру любым способом (свой `Standoff3.bat`, или в браузере адрес `http://IP_ХОСТА:8765`).
2. «🌐 Игра по сети» → «Присоединиться» → впишите ник и адрес `ws://IP_ХОСТА:8766` (IP хост сообщит из своего окна сервера) → «Подключиться».
3. Дождитесь, пока хост начнёт матч.

Хост считает всю игру (физику, попадания, ботов, раунды) — так честнее и надёжнее по сети; у остальных клиент лёгкий.
**Ограничения этой версии:** только локальная сеть, без матчмейкинга; подключиться можно только до старта матча (не на ходу);
брошенное на землю оружие не синхронизируется между игроками; голосового чата нет.

## Кейсы и скины
Пункт меню «🎁 Кейсы и скины». За каждый завершённый матч начисляется золото (150 + 8 за убийство + 4 за хедшот + 120 за победу);
на старте выдаётся 2000 золота. Пять кейсов (новичка, штурмовой, снайперский, ножевой, золотой), 390 скинов шести редкостей:
Обычный → Необычный → Редкий → Эпический → Легендарный → Арканный. Открытие — рулетка как в CS; выпавший скин можно надеть или продать за золото.
Надетые скины видны от первого лица, на моделях персонажей, на выброшенном оружии и другим игрокам по сети (боты тоже ходят со случайными скинами).
**Ножи** — 9 видов с настоящими моделями: штык-нож, флип, танто, стилет, байонет, боуи, кукри, керамбит и бабочка; каждый вид имеет свою редкость,
а финиш накладывается сверху (кейс ножей и золотой кейс). Инвентарь хранится в localStorage браузера.

## Версии
- **3.0.0** — одиночная игра с ботами, 4 карты, 4 режима.
- **3.1.0** — мультиплеер по локальной сети, никнеймы, исправлена анимация ботов.
- **3.2.0** — кейсы и скины, 3D-превью оружия в меню, исправлено положение оружия в руках персонажей.
- **3.3.0** — десктоп-версия: Standoff3.exe (Electron) и загрузчик для macOS, адрес VPS-сервера в config.json, скрипт установки релея на VPS.
- **3.4.0** — консоль (`), игра вместо бота по E (и по сети), окружение за пределами карт (город, промзона, горы, облака), более светлые Rust и Arena; исправлен выход из сетевого матча (хост освобождает сервер).
- **3.7.0** — главное меню в стиле CS:GO: 3D-сцена с вашим персонажем, надетыми скинами и сменой оружия; оптимизация: exe теперь использует дискретную видеокарту (GeForce/Radeon) вместо встроенной, персонажи и окружение объединены в меньше draw-call'ов (≈1000 → ≈350 на кадр), теневой проход через кадр, PCF-тени на среднем качестве, без bump-карт на низком.
- **3.6.0** — комнаты на сервере: несколько компаний играют на одном VPS одновременно, список открытых комнат при подключении; клиент может зайти раньше хоста; адрес сервера и в браузерной версии.
- **3.5.0** — 9 видов ножей с настоящими моделями (нож больше не «кирпич»), F11 работает везде + кнопка «Полный экран» в меню, секретная консольная команда.

## Консоль и управление ботами
Клавиша <code>`</code> (Ё) открывает консоль как в CS. `help` — список команд. Основное: `map <карта> [режим]`, `bot_add [ct|t] [сложность]`,
`bot_kick <имя|all>`, `bot_difficulty <easy|medium|hard|expert>`, `status`, `say <текст>`, `connect ws://адрес:8766`, `disconnect`,
`sensitivity`, `fov`, `name`, `fps`, `skins` / `skin <id>`. Читы (`give`, `money`, `hp`, `armor`, `god`, `noclip`, `respawn`) работают только в одиночной игре.

**Играть вместо бота:** после смерти переключайте наблюдение ЛКМ на бота своей команды и нажмите **E** — вы займёте его место
(его оружие, здоровье и позиция). Работает и в сетевой игре: хост подтверждает замену, бот исчезает с карты до следующего раунда.

## Если игра лагает
- В exe дискретная видеокарта включается сама. В браузерной версии Chrome может выбрать встроенную: Windows → Параметры → Система → Дисплей → Графика → добавить Chrome → «Высокая производительность».
- Настройки → Качество «Среднее» (PCF-тени 2048) или «Низкое» (без bump-карт, тени можно выключить), Масштаб рендера 1.0.

## Технологии
Three.js r158 (lib/three.min.js). Все текстуры, модели, скины и звуки генерируются процедурно в самой игре (Canvas + Web Audio).
Сетевой релей-сервер (`server.py`) — чистый Python без внешних зависимостей (свой WebSocket-хендшейк и фреймы).
Настройки, статистика и инвентарь скинов сохраняются в localStorage браузера.
